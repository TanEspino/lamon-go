// Shell component required by Expo Router for the center create tab.
// The actual navigation press is intercepted to open the uploader modal,
// so this screen is never mounted or rendered.
export default function CreateTabPlaceholder() {
    return null;
}
